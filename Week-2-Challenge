function minMax(arr) {
	minimum = arr[0];
	maximum = arr[0];
	
	for (let i = 0; i < arr.length; i++) {
		if (arr[i] < minimum) {
			minimum = arr[i];
		}
		if (arr[i] > maximum) {
			maximum = arr[i];
		}
	}
	
	return [minimum, maximum];
}
